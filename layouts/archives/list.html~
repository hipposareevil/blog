{{ define "main" }}

<section class="mx-auto max-w-4xl px-4">
  <h1 class="text-4xl font-extrabold mb-8">
    {{ .Title }}
  </h1>

  {{ $pages := where .Site.RegularPages "Type" "in" (slice "posts") }}
  {{ $grouped := $pages.GroupByDate "2006" }}

  {{ range $grouped }}
    <div class="mb-10">
      <h2 class="text-2xl font-bold mb-4">
        {{ .Key }}
      </h2>

      <ul class="space-y-2">
        {{ range .Pages }}
          <li class="flex items-baseline gap-3">
            <span class="text-sm text-neutral-400 w-20">
              {{ .Date.Format "Jan 02" }}
            </span>
            <a
              href="{{ .RelPermalink }}"
              class="font-medium hover:underline">
              {{ .Title }}
            </a>
          </li>
        {{ end }}
      </ul>
    </div>
  {{ end }}

</section>

{{ end }}
